<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jam2in.arcus.board.repository.CommentRepository">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comment (user_id, post_id, content)
        VALUES ((SELECT id FROM user ORDER BY RAND() LIMIT 1), #{post_id}, #{content})
    </insert>

    <update id="update">
        UPDATE comment
        SET content = #{content}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM comment
        WHERE id = #{id}
    </delete>

    <select id="selectAll">
        SELECT * FROM comment
        WHERE comment.post_id = #{post_id}
    </select>

    <select id="selectOne">
        SELECT * FROM comment
        WHERE id = #{id}
    </select>
</mapper>